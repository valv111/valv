{
  "name": "clipbridge",
  "version": "1.2.0",
  "description": "局域网共享剪贴板，打开同一地址即可实时同步文本",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/smoke.test.js",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  }
}
