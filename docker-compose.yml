services:
  clipbridge:
    build: .
    container_name: clipbridge
    ports:
      - "${PORT:-3456}:3456"
    environment:
      - PORT=3456
      - HOST=0.0.0.0
      # 可选：设置访问密码，留空则无需密码
      - PIN=${PIN:-}
    restart: unless-stopped
